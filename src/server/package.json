{
  "name": "server",
  "version": "1.0.0",
  "description": "Server part of the project",
  "main": "dist/server.js",
  "scripts": {
    "start": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/server.ts",
    "build": "tsc"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "@types/express": "^4.17.11",
    "typescript": "^4.1.3",
    "ts-node": "^9.1.1",
    "nodemon": "^2.0.7"
  }
}
